[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "sceneagent"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = [".."]
include = ["sceneagent*"]
